<!-- src/main/resources/META-INF/plugin.xml -->
<idea-plugin url="https://github.com/adobe/ClawDEA">
    <id>com.adobe.clawdea</id>
    <name>ClawDEA</name>
    <vendor url="https://github.com/adobe/ClawDEA" email="spopescu@adobe.com">ClawDEA</vendor>
    <description><![CDATA[
<p><b>ClawDEA gives <a href="https://docs.anthropic.com/en/docs/claude-code">Claude Code</a>
first-class access to everything IntelliJ already knows about your project</b> &mdash; the PSI index,
the debugger, the profiler, compiler diagnostics &mdash; so Claude stops guessing with grep and starts
reasoning with the same tools you do.</p>

<p>Most Claude plugins wrap the CLI in a chat window. ClawDEA goes further: a local MCP server exposes
IntelliJ's indices, debugger, and JFR profiler as tools Claude can call. The result is an AI pair programmer
that finds symbols by reference instead of regex, steps through a failing test to see the actual values,
and proposes source-level fixes from a CPU or allocation profile.</p>

<p><b>See it in action:</b> the <a href="https://github.com/adobe/ClawDEA#clawdea">README</a> has a live
demo of Claude autonomously debugging a Java application &mdash; setting breakpoints, stepping, inspecting
variables, and mutating runtime state to verify hypotheses.</p>

<h2>What makes ClawDEA different</h2>

<ul>
  <li><b>IntelliJ's PSI index as MCP tools.</b> <code>find_symbol</code>, <code>find_usages</code>,
  <code>find_callers</code>, <code>find_implementations</code>, <code>find_supertypes</code>,
  <code>resolve_symbol</code>, scoped <code>search_text</code>, and live compiler diagnostics.
  Precise (no false hits in comments or strings), complete (finds aliased imports and renamed refs),
  and faster than any grep-based plugin.</li>

  <li><b>Live debugger over MCP &mdash; 21 tools.</b> Claude can launch run configurations, attach to
  processes, set conditional and log breakpoints, step in/over/out, run to cursor, inspect frames, expand
  composite variables, evaluate expressions, and mutate values at runtime to test fix ideas without
  recompiling. Breakpoint ownership tracking means your breakpoints are never touched.</li>

  <li><b>JVM profiling as a diagnostic loop.</b> One-click profiling of tests and run configurations via
  JDK Flight Recorder. Claude analyzes CPU hotspots, allocation pressure, and memory leaks (.jfr and .hprof),
  then proposes source-level fixes. Entry points: <code>/profile</code> slash command, gutter icon on
  <code>@Test</code> methods, or imported recordings.</li>

  <li><b>Project knowledge layer.</b> A project-local wiki under <code>.claude/wiki/</code> plus an
  auto-generated <code>REPO_STATE.md</code>. The primer ships with every turn so Claude starts each
  conversation oriented &mdash; no "let me explore the codebase first". Personal notes via <code>/note</code>
  can be promoted to shared concept pages. Cross-repo navigation for monorepos via
  <code>.clawdea-workspace.md</code> and sibling-repo tools.</li>
</ul>

<h2>The rest of the toolkit</h2>

<ul>
  <li><b>Native edit review.</b> Each proposed change opens IntelliJ's diff dialog with Accept/Reject.
  A fallback coordinator catches built-in Edit/Write calls that bypass MCP and adds inline buttons with
  file revert, so Claude cannot silently overwrite your work.</li>

  <li><b>Inline completions.</b> Tab-completions powered by the Claude API with editor context from the
  context engine. Falls back to <code>claude -p</code> when no API key is configured.</li>

  <li><b>Intention actions (Alt+Enter).</b> Explain, Optimize, Generate Test, Security Check, Add
  Documentation, Refactor with Instructions, Ask Claude, Fix with Claude.</li>

  <li><b>Chat panel.</b> Streams responses with Markdown, tool-use cards, and clickable code references
  that navigate to source. Supports Auto, Plan, and Ask modes. <code>@</code>-mention for inline file and
  symbol picking, backed by IntelliJ's filename and short-name caches.</li>

  <li><b>Tool-call permissions.</b> Three modes (Confirm all / Allow safe / Allow all), with honoring of
  Claude Code's <code>permissions.allow</code> / <code>permissions.deny</code> rules. Permission cards
  block the CLI inline until you decide.</li>

  <li><b>Slash commands + Claude Code skills.</b> Built-ins (<code>/stop</code>, <code>/clear</code>,
  <code>/mode</code>, <code>/cost</code>, <code>/compact</code>, <code>/resume</code>, <code>/profile</code>,
  <code>/callers</code>, <code>/usages</code>, <code>/implementations</code>, <code>/supertypes</code>,
  <code>/note</code>, <code>/promote-to-wiki</code>) plus Claude Code skills discovered at runtime.</li>

  <li><b>Session resume.</b> Pick up a previous session with conversation history replayed in the chat panel.</li>

  <li><b>Auth flexibility.</b> API key, Claude subscription sign-in, AWS Bedrock, or Google Vertex.</li>
</ul>

<h2>Staying in sync with Claude Code</h2>

<p>ClawDEA wraps the Claude Code CLI as a subprocess and runs a weekly drift-monitoring job that diffs
<code>claude --help</code>, npm versions, and selected docs against a moving snapshot, plus a PR-time
fixture-replay test over a recorded stream-json transcript. Upstream changes to the CLI get caught by
CI, not by users.</p>

<h2>Getting started</h2>

<ol>
  <li>Install the CLI: <code>npm install -g @anthropic-ai/claude-code</code></li>
  <li>Install this plugin.</li>
  <li>Open <b>Settings &rarr; Tools &rarr; ClawDEA</b> and configure an Anthropic API key, sign in with a
  Claude subscription, or configure AWS Bedrock / Google Vertex credentials.</li>
  <li>Open the chat panel (<b>Tools &rarr; Toggle ClawDEA Chat</b>) and start coding.</li>
</ol>

<h2>Requirements</h2>

<ul>
  <li>IntelliJ IDEA 2026.1+</li>
  <li>Java 21 on the IDE's JBR (bundled)</li>
  <li>Claude Code CLI installed</li>
  <li>Anthropic API key, Claude subscription, or AWS Bedrock / Google Vertex credentials</li>
</ul>

<p><a href="https://github.com/adobe/ClawDEA">GitHub</a>
&middot; <a href="https://github.com/adobe/ClawDEA/blob/main/docs/user-guide.md">User guide</a>
&middot; <a href="https://github.com/adobe/ClawDEA/issues">Report an issue</a></p>

<p><i>ClawDEA is licensed under Apache 2.0. "Claude" and "Claude Code" are trademarks of Anthropic, PBC.
"IntelliJ", "IntelliJ IDEA", and "JetBrains" are trademarks of JetBrains s.r.o. ClawDEA is an independent
integration and is not affiliated with or endorsed by Anthropic or JetBrains.</i></p>
    ]]></description>

    <change-notes><![CDATA[
<h3>1.4.1</h3>
<p><b>Auto-update wiki now actually edits the wiki.</b> When you turn on "Auto-update wiki on drift", the wiki-author subagent now lands its proposed changes to disk in the background. Previously the subagent ran but its edits never reached your wiki because it was waiting for a diff-approval dialog that nobody could see.</p>
<ul>
  <li>Recording wiki suggestions ("missingConcept", "staleConcept", "incompleteConcept" via the librarian) is now reliable &mdash; previously failed silently for some inputs</li>
  <li>The wiki-author runs in fully unattended mode, has access to the project's IntelliJ index tools (find_symbol, search_text, etc.), and inherits your auth (subscription / Bedrock / Vertex) just like the chat</li>
  <li>Failures are now logged with enough context (exit code, stderr) to tell you why a wiki update didn't land instead of failing silently</li>
</ul>

<h3>1.4.0</h3>
<h4>Wiki knowledge layer</h4>
<ul>
  <li><b>Agent-based wiki:</b> replaced system-prompt-based wiki usage with specialized sub-agents <i>wiki-librarian</i> and <i>wiki-author</i> &mdash; slimmer system prompt, always-fresh findings (no long-session decay), supports larger wikis with better coverage on big projects</li>
  <li><b>Wiki-librarian routing:</b> Claude now consults the wiki-librarian subagent before answering project-understanding questions, grounding answers in synthesized concept pages with verified source citations</li>
  <li><b>Wiki-author subagent:</b> replaces the dream-based wiki maintenance system with a commit-driven drift detector that identifies stale wiki claims from git history, and a wiki-author subagent that rewrites affected pages</li>
  <li><b>Drift banner suppression:</b> with "Auto-update wiki" enabled, wiki-author events are handled silently without surfacing the drift banner; only truly unresolvable events remain visible</li>
</ul>
<h4>Permission UX</h4>
<ul>
  <li><b>Multi-panel routing:</b> permission cards now appear in the chat tab that triggered the tool call instead of whichever tab is focused, fixing wrong-tab approvals in multi-tab projects</li>
  <li><b>Auto-allowed badge:</b> the "⚡ Auto-allowed" marker now shows reliably on fast tool calls (it was racing with the HTTP signal on sub-second tools)</li>
  <li><b>Compact permission card:</b> removed redundant tool name and input echo since that info is already visible in the tool header above</li>
</ul>

<h3>1.3.6</h3>
<ul>
  <li><b>Windows: CLI permission settings no longer crash the CLI</b> &mdash; cmd.exe was stripping double-quotes from <code>.cmd</code> shim arguments, mangling the inline permission JSON; settings are now passed via a temp file</li>
  <li><b>Windows: tool-call panels show just the filename</b> for Read/Write/Edit actions, matching macOS behavior (was showing the full <code>C:\\...</code> path)</li>
</ul>

<h3>1.3.5</h3>
<ul>
  <li><b>Windows: ClawDEA now finds the Claude CLI when IntelliJ is launched from the Start menu/Explorer</b> &mdash; probes <code>%APPDATA%\\npm</code>, <code>%LOCALAPPDATA%\\Volta\\bin</code>, <code>%USERPROFILE%\\.local\\bin</code>, and <code>C:\\Program Files\\nodejs</code>. PowerShell shims are transparently redirected to the sibling <code>.cmd</code></li>
  <li><b>Windows: <code>.cmd</code>/<code>.bat</code> shims now launch correctly</b> &mdash; the executability check no longer rejects them (Windows decides launchability via <code>PATHEXT</code>, not the executable bit)</li>
  <li><b>Large system prompts no longer overflow the OS command-line limit</b> &mdash; the appended system prompt is now written to a temp file and passed via <code>--append-system-prompt-file</code>, fixing crashes on Windows (32 KB limit) and risk on Linux (<code>ARG_MAX</code>) when the skill catalog or primer grows large</li>
  <li><b>Release workflow now attaches the signed plugin zip to GitHub releases</b> automatically</li>
</ul>

<h3>1.3.4</h3>
<ul>
  <li>Compatibility fix for JetBrains Marketplace: profiling no longer uses an internal API flagged by Marketplace moderation</li>
</ul>

<h3>1.3.3</h3>
<ul>
  <li>Refreshed plugin icons (Marketplace thumbnail + tool-window, light and dark)</li>
  <li>First JetBrains Marketplace release</li>
</ul>

<h3>1.3.2</h3>
<ul>
  <li>Knowledge-layer reasoning: path-aware wiki probe, invariant-first concept pages, correction capture</li>
  <li>New <code>find_symbol</code> MCP tool &mdash; resolve class/method/field names to definition locations via the PSI index</li>
  <li>Per-session tool-approval state fixed</li>
  <li>Stronger tool-routing guidance steers Claude toward the index tools and away from filesystem grep for code symbols</li>
</ul>

<h3>1.2.0</h3>
<ul>
  <li>JFR-backed profiling: CPU, allocations, memory leaks, .jfr/.hprof import, gutter icon on <code>@Test</code> methods</li>
</ul>

<h3>1.1.0</h3>
<ul>
  <li>Hardened tool-permission UX and CLI session reliability</li>
  <li>Drift monitoring for Claude Code CLI changes with weekly digest + PR-time fixture replay</li>
</ul>

<h3>1.0.0</h3>
<ul>
  <li>Initial public release for IntelliJ IDEA 2026.1</li>
</ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>
    <depends>Git4Idea</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Settings -->
        <applicationService
            serviceImplementation="com.adobe.clawdea.settings.ClawDEASettings"/>

        <!-- Auth Manager -->
        <applicationService
            serviceImplementation="com.adobe.clawdea.auth.AuthManager"/>

        <applicationConfigurable
            parentId="tools"
            instance="com.adobe.clawdea.settings.ClawDEASettingsConfigurable"
            id="com.adobe.clawdea.settings"
            displayName="ClawDEA"/>

        <!-- Context Engine -->
        <projectService
            serviceImplementation="com.adobe.clawdea.context.ContextEngine"/>

        <!-- Filesystem Refresh Coordinator -->
        <projectService
            serviceImplementation="com.adobe.clawdea.chat.FilesystemRefreshCoordinator"/>

        <!-- Chat Auto-Resume State -->
        <projectService
            serviceImplementation="com.adobe.clawdea.chat.session.ChatAutoResumeState"/>

        <!-- MCP Server -->
        <projectService
            serviceImplementation="com.adobe.clawdea.mcp.McpServer"/>

        <!-- Debug Bridge -->
        <projectService
            serviceImplementation="com.adobe.clawdea.debug.DebugBridge"/>

        <!-- Drift Detection -->
        <projectService
            serviceImplementation="com.adobe.clawdea.knowledge.drift.DriftDetectionService"/>
        <postStartupActivity
            implementation="com.adobe.clawdea.knowledge.drift.DriftStartupActivity"/>
        <postStartupActivity
            implementation="com.adobe.clawdea.mcp.McpServerStartupActivity"/>

        <!-- Git Repository Change Subscriber (drives drift rescan on git events) -->
        <projectService
            serviceImplementation="com.adobe.clawdea.chat.GitRepositoryChangeSubscriber"/>
        <postStartupActivity
            implementation="com.adobe.clawdea.chat.GitRepositoryChangeSubscriberStartupActivity"/>

        <!-- Claude Gateway -->
        <applicationService
            serviceImplementation="com.adobe.clawdea.gateway.ClaudeGateway"/>

        <!-- Inline Completions -->
        <inline.completion.provider
            implementation="com.adobe.clawdea.completions.ClawDEACompletionProvider"/>

        <!-- Intention Actions (Alt+Enter) -->
        <intentionAction>
            <className>com.adobe.clawdea.actions.intentions.ExplainCodeAction</className>
            <category>ClawDEA</category>
        </intentionAction>
        <intentionAction>
            <className>com.adobe.clawdea.actions.intentions.OptimizeCodeAction</className>
            <category>ClawDEA</category>
        </intentionAction>
        <intentionAction>
            <className>com.adobe.clawdea.actions.intentions.GenerateTestAction</className>
            <category>ClawDEA</category>
        </intentionAction>
        <intentionAction>
            <className>com.adobe.clawdea.actions.intentions.SecurityCheckAction</className>
            <category>ClawDEA</category>
        </intentionAction>
        <intentionAction>
            <className>com.adobe.clawdea.actions.intentions.AddDocumentationAction</className>
            <category>ClawDEA</category>
        </intentionAction>
        <intentionAction>
            <className>com.adobe.clawdea.actions.intentions.RefactorWithInstructionsAction</className>
            <category>ClawDEA</category>
        </intentionAction>
        <intentionAction>
            <className>com.adobe.clawdea.actions.intentions.AskClaudeAction</className>
            <category>ClawDEA</category>
        </intentionAction>
        <intentionAction>
            <className>com.adobe.clawdea.actions.quickfix.FixWithClaudeIntention</className>
            <category>ClawDEA</category>
        </intentionAction>

        <!-- Chat Tool Window -->
        <toolWindow
            id="ClawDEA"
            anchor="right"
            secondary="true"
            canCloseContents="true"
            icon="/icons/clawdea.svg"
            factoryClass="com.adobe.clawdea.chat.ChatToolWindowFactory"/>

        <!-- Notifications -->
        <notificationGroup
            id="ClawDEA"
            displayType="BALLOON"/>

        <!-- Profiling Line Marker -->
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="com.adobe.clawdea.profiling.ui.ProfilingLineMarker"/>
    </extensions>

    <actions>
        <!-- Context Menu Group -->
        <group id="ClawDEA.EditorPopupMenu"
               class="com.adobe.clawdea.actions.menu.ClawDEAActionGroup"
               text="ClawDEA"
               popup="true">
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
        </group>

        <action id="ClawDEA.ToggleChatPanel"
                class="com.adobe.clawdea.actions.shortcuts.ToggleChatPanelAction"
                text="Toggle ClawDEA Chat"
                description="Toggle focus between editor and ClawDEA chat panel">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>

        <action id="ClawDEA.NewChatSession"
                class="com.adobe.clawdea.actions.shortcuts.NewChatSessionAction"
                text="New ClawDEA Chat Session"
                description="Open a new ClawDEA chat session tab">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>

        <action id="ClawDEA.AbortResponse"
                class="com.adobe.clawdea.actions.shortcuts.PauseOrAbortAction"
                text="Pause or Abort ClawDEA Response"
                description="Pause the current Claude response; press again to fully abort">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>

        <action id="ClawDEA.ProfileRun" class="com.adobe.clawdea.profiling.ui.ProfilingRunAction"
            text="Run with ClawDEA Profiler" description="Profile the selected Run configuration">
            <add-to-group group-id="RunMenu" anchor="last"/>
        </action>
        <action id="ClawDEA.ImportProfile" class="com.adobe.clawdea.profiling.ui.ImportProfileAction"
            text="Analyze with ClawDEA" description="Analyze this recording with ClawDEA profiler">
            <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
